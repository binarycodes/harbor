# The library's storage, kept across `./run.sh env down` and thrown away only by
# `./run.sh env reset`.
[Unit]
Description=Harbor development database storage

[Volume]
# As with the network: named plainly rather than systemd-harbor-dev-data.
VolumeName=harbor-dev-data
